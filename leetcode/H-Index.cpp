#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

//int hIndex(vector<int>& citations) {
//    sort(citations.begin(), citations.end(), greater<int>());
//    int i = 0;
//    for(; i < citations.size(); i++)
//        if(citations[i] < i+1)
//            break;
//    return i;
//}

//int main() {
//    vector<int> ci = {5,3,1,6,0,2};
//    int res = hIndex(ci);
//    return 0;
//}
